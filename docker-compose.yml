services:
  praktor:
    container_name: praktor
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    group_add:
      - "${DOCKER_GID:-131}"
    volumes:
      - ./config/praktor.yaml:/etc/praktor/praktor.yaml:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - praktor-data:/data
      - praktor-global:/data/agents/global
    environment:
      - TZ=Europe/Athens
      - PRAKTOR_CONFIG=/etc/praktor/praktor.yaml
      - PRAKTOR_TELEGRAM_TOKEN=${PRAKTOR_TELEGRAM_TOKEN}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - CLAUDE_CODE_OAUTH_TOKEN=${CLAUDE_CODE_OAUTH_TOKEN}
      - PRAKTOR_WEB_PASSWORD=${PRAKTOR_WEB_PASSWORD}
      - PRAKTOR_VAULT_PASSPHRASE=${PRAKTOR_VAULT_PASSPHRASE}
    networks:
      - praktor-net
    restart: unless-stopped

volumes:
  praktor-data:
    name: praktor-data
  praktor-global:
    name: praktor-global

networks:
  praktor-net:
    name: praktor-net
    driver: bridge
